import { NextRequest } from "next/server"; import prisma from "@/lib/db"; import { requireAuth, tenantFilter, ok, unauthorized, notFound, serverError } from "@/lib/auth"; export async function GET(req: NextRequest, { params }: { params: Promise<{ id: string }> }) { try { const payload = requireAuth(req); if (!payload) return unauthorized(); const { id } = await params; const c = await prisma.legalCase.findFirst({ where: { id, ...(payload.tenantId ? { tenantId: payload.tenantId } : {}) }, include: { client: { select: { id: true, companyName: true } } } }); if (!c) return notFound("پرونده"); return ok(c); } catch (e) { return serverError(e); } } export async function PUT(req: NextRequest, { params }: { params: Promise<{ id: string }> }) { try { const payload = requireAuth(req); if (!payload) return unauthorized(); const { id } = await params; const body = await req.json(); const c = await prisma.legalCase.update({ where: { id }, data: { title: body.title, type: body.type, status: body.status, plaintiff: body.plaintiff, defendant: body.defendant, court: body.court, lawyerId: body.lawyerId, clientId: body.clientId, nextHearing: body.nextHearing ? new Date(body.nextHearing) : undefined, description: body.description, actions: body.actions } }); return ok(c); } catch (e) { return serverError(e); } } export async function DELETE(req: NextRequest, { params }: { params: Promise<{ id: string }> }) { try { const payload = requireAuth(req); if (!payload) return unauthorized(); const { id } = await params; const c = await prisma.legalCase.findFirst({ where: { id, ...(payload.tenantId ? { tenantId: payload.tenantId } : {}) } }); if (!c) return notFound("پرونده"); await prisma.legalCase.delete({ where: { id } }); return ok({ message: "پرونده حذف شد" }); } catch (e) { return serverError(e); } }