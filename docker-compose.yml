version: "3.9"

services:
  mysql:
    image: mysql:8.0
    container_name: persicore_mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root1234
      MYSQL_DATABASE: persicore_crm
      MYSQL_USER: persicore
      MYSQL_PASSWORD: persicore123
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci

volumes:
  mysql_data:
